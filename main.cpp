#include<iostream>
#include<math.h>
using namespace std;

int main()
{
    int n n_for_digit;
    char b;
    char *result;
    int digit = 0; //자릿수

    int processed_b = int(*b)

    if(processed_b >= 65)
    {
        processed_b = b - 55;
    }
    else
    {
        processed_b = b - 48;
    }

    cin >> n >> b;

    n_for_digit = n;

    while(n_for_digit > 0)
    {
        n_for_digit /= 10;
        digit++;
    }

    while()
    {
        result = n / processed_b;

    }


    
    return 0;
}